# SSH Tunnel Connect

This is a simple Node.js application that creates an SSH connection to a remote server.

## Features
- Uses `ssh2` library to connect via SSH
- Basic setup for tunneling and secure access
- Prepared for deployment or testing

## How to Run
```bash
npm install
node app.js
```

Make sure to update your SSH credentials in `app.js` before running.
